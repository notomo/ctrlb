language:
    node_js
node_js:
    - 'node'
sudo: false
install:
    - npm ci
    - npm install -g codecov
script:
    - npm run build && npm run lint
    - npm run coverage && codecov
cache:
    directories:
        - "$HOME/.npm"
